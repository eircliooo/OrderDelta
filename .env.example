# 外贸订单差异雷达 — 环境变量示例
#
# 复制为 .env 后生效。**所有项都有安全的默认值，不配置也能跑。**

# ---------------------------------------------------------------- 监听地址
#
# ⚠️ 默认 127.0.0.1（只有本机能访问）。
#
# **改成 0.0.0.0 会让同一局域网内任何人无需密码打开你的订单数据。**
# 本工具 MVP 阶段没有任何身份认证——回环绑定就是唯一的访问控制手段。
# 如果你确实需要局域网访问，请自行在前面加一层带认证的反向代理，
# 不要直接把这里改成 0.0.0.0。
ORDERDELTA_HOST=127.0.0.1
ORDERDELTA_PORT=8000

# ---------------------------------------------------------------- 数据位置
#
# 数据库与上传的原始文件都放在这里。删除这个目录 = 删除全部数据。
# 默认是仓库根目录下的 data/。
# ORDERDELTA_DATA_DIR=./data

# ---------------------------------------------------------------- 上传限制
#
# 单个文件大小上限（字节）。默认 20MB。
# ORDERDELTA_MAX_UPLOAD_BYTES=20971520

# ---------------------------------------------------------------- 外部模型
#
# MVP-0 **根本不调用任何外部模型**，也不需要任何 API Key。
# 文件全程留在本机，没有遥测，没有第三方分析。
# LLM 字段映射是 MVP-1 的可选扩展点，届时必须靠**显式开关**启用——
# 「环境变量里恰好有 key」不构成启用条件。

# ---------------------------------------------------------------- 中国大陆网络
#
# pip / npm 直连可能很慢。可选镜像（在各自目录下执行，不用写进 .env）：
#
#   pip:  backend\.venv\Scripts\python.exe -m pip config set global.index-url \
#           https://pypi.tuna.tsinghua.edu.cn/simple
#   npm:  npm config set registry https://registry.npmmirror.com
#   winget 装 Python 需要指定源（msstore 源会超时）：
#           winget install --id Python.Python.3.12 --source winget
