# 全仓强制 LF。**不是风格偏好，是两处会真的红掉的地方：**
#
# 1. `fixtures/expected/*.json` 有一条逐字节断言
#    （`test_仓库内的fixture就是生成器的产物`）：仓库内的产物必须等于
#    `tools/fixtures/build.py` 当场生成的字节，而生成器写的是 `newline="\n"`。
#    Windows 默认 `core.autocrlf=true` 会在 **checkout 时**把它们转成 CRLF，
#    于是新克隆的第一次 `pytest` 必红，且失败原因与本人的改动毫无关系——
#    最容易被误判成「这套测试本来就不稳」然后被删掉。
# 2. `scripts/verify.sh`：CRLF 行尾的 shebang 脚本在 bash 下直接失败。
* text=auto eol=lf

# xlsx 是 zip，任何行尾转换都会破坏它，且同样有 sha256 逐字节断言。
*.xlsx binary
